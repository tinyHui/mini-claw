# Pi Packages

This directory is reserved for future local Pi packages.

Each child should be an independently understandable package with its own
manifest and conventional Pi resource directories as needed. Add a package to
`.pi/settings.json` only when it is ready to be loaded by this workspace.

No package is included or installed during the cleanup phase.
