# Sandbox (mini-claw)

Source for the Mini-Claw sandbox integration lives in **`src/extensions/sandbox/`** (TypeScript package with `index.ts` barrel).

This folder mirrors the conventional “extensions/sandbox” layout; the compiled output is emitted to `dist/extensions/sandbox/` when you run `pnpm build`.
